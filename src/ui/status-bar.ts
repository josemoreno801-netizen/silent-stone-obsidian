/**
 * Status bar display — shows sync state in Obsidian's status bar.
 *
 * States:
 *   SS: Synced        — All files in sync
 *   SS: Syncing...    — Active upload/download
 *   SS: 3 conflicts   — Unresolved conflicts
 *   SS: Offline       — Server unreachable
 *   SS: Not connected — No server configured
 *
 * TODO: Implement
 * - Color-coded status indicators
 * - Click to trigger manual sync
 * - Tooltip with last sync time and file counts
 */

export class StatusBarDisplay {
  // Placeholder — status bar logic is currently in main.ts
}
